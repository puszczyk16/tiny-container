apiVersion: v1
kind: Service
metadata:
  labels:
    app: tiny
  name: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: http
    name: http
  selector:
    app: tiny-{{ .Release.Name }}
